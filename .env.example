# AWS S3 credentials (not currently used by tests, which run against a local s3rver mock).
# Set these if you add integration tests against a real S3 bucket in the future.
#
# AWS_S3_BUCKET=my-test-bucket
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
